<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wheelsreputation">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>

    <application
        android:name=".global_variable"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WHEELSREPUTATION">
        <activity
            android:name=".registration_screen_2"
            android:exported="false" />
        <activity
            android:name=".registration_page_0ne"
            android:exported="false" />
        <activity
            android:name=".Add_something"
            android:exported="false" />
        <activity
            android:name=".login_Activity"
            android:exported="false" />
        <activity
            android:name=".Stolen_bike_list"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".Find_driver_activity"
            android:exported="false" />
        <activity
            android:name=".Driver_reputation_list"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>